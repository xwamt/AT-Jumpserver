# AT JumpServer Terminal

Standalone VS Code extension for JumpServer-backed SSH terminal sessions.

This repository is being built from the AT Terminal UI baseline. The first version keeps the terminal webview experience and replaces the direct SSH backend with a JumpServer REST plus KoKo WebSocket transport.
